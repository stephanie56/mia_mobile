import React, { Component } from 'react';
import { Text, View, StyleSheet } from 'react-native';
import MainPage from './scenes/GreetingPage';

export default class App extends Component {
  render() {
    return (
      <MainPage />
    );
  }
}
